---
interface Props {
  content: string;
  position?: 'left' | 'right';
  rotation?: number;
  icon?: string;
}

const { 
  content, 
  position = 'right', 
  rotation = Math.random() * 8 - 4, // Random rotation between -4 and 4 degrees
  icon = '⚡' 
} = Astro.props as Props;

const rotationStyle = `transform: rotate(${rotation}deg);`;
const positionClass = position === 'left' ? 'left-4' : 'right-4';
---

<div 
  class={`action-badge absolute top-2 ${positionClass} max-w-48 z-10 print:block`}
  style={rotationStyle}
>
  <div class="bg-white border-2 border-dashed border-red print:border-black p-3 shadow-lg print:shadow-none">
    <div class="flex items-start space-x-2">
      <div class="text-red print:text-black text-xl flex-shrink-0 mt-0.5">
        {icon}
      </div>
      <div class="text-sm text-ink font-medium leading-tight font-body">
        <div class="text-red print:text-black font-mono font-bold text-xs uppercase tracking-widest mb-1">
          Spice it up!
        </div>
        <div set:html={content} />
      </div>
    </div>
  </div>
</div>

<style>
  .action-badge {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  }
  
  @media print {
    .action-badge {
      position: absolute;
      z-index: 10;
    }
  }
</style>